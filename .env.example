# Copy to .env and fill in what you use:
#   cp .env.example .env

# Option A — OpenAI cloud (default in config.json: provider=openai, model=gpt-5.4-nano)
# OPENAI_API_KEY=sk-...

# Option B — free local via Ollama (set provider=local + model in solution/config.json)
LOCAL_BASE_URL=http://127.0.0.1:11434/v1
LOCAL_MODEL=phi3:latest
